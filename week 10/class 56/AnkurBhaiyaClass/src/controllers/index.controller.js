

module.exports.indexControllers = (req, res) => {               //(res,req)=>{} controller
    res.send("hellow")
}

module.exports.aboutControllers = (req, res) => {               //(res,req)=>{} controller
    res.send("aboutPage")
}