<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Uploaded Files</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-r from-blue-500 to-purple-600 min-h-screen flex justify-center items-center">

    <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-md">
        <h2 class="text-2xl font-bold text-center text-gray-800 mb-6">Uploaded Images</h2>

        <div class="grid grid-cols-2 gap-4">
            <% if (files && files.length > 0) { %>
                <% files.forEach(file => { %>
                    <div class="flex justify-center">
                        <img src="/images/uploads/<%= file %>" alt="<%= file %>" class="w-32 h-32 object-cover rounded-md"/>
                    </div>
                <% }) %>
            <% } else { %>
                <p class="text-center text-gray-600">No files uploaded yet.</p>
            <% } %>
        </div>

        <div class="mt-6 text-center">
            <a href="/" class="py-2 px-4 bg-blue-500 text-white rounded-md font-semibold hover:bg-blue-600 transition duration-300">
                Back to Home
            </a>
        </div>
    </div>

</body>
</html>
